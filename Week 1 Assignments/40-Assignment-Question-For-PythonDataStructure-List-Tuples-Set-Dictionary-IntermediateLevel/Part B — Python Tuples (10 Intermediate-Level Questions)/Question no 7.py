# 7. Extract the last three items from a tuple using slicing.

numbers = (2, 5, 2, 8, 2, 3, 5)

print(f"Tuple of numbers: {numbers}")

print(f"Extract the last three items from a tuple using slicing: {numbers[len(numbers)-3: len(numbers)]}")