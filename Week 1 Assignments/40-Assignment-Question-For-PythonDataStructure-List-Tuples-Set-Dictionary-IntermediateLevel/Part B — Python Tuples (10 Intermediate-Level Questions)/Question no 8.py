# 8. Concatenate a tuple with itself three times (repeat operation).

numbers = (2, 5, 2, 8, 2, 3, 5)

print(f"Tuple of numbers: {numbers}")

numbers = numbers + numbers + numbers

print(f"After concatenating a tuple with itself three times: {numbers}")